# Benchmarks (short index)

The exhaustive measurement write-up is [Chapter 6 — Benchmarks and profiling](06_BENCHMARKS_AND_PROFILING.md).

Interactive menu, CSV paths, and `plot_metrics.py` are described there. Operator-level Custom vs Torch timing is `bench_micro_ops`.
